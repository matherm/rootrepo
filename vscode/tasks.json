{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug-current-file-in-docker",
            "type": "shell",
            "command": "docker exec -d box python -m ptvsd --host 0.0.0.0 --port 5678 --wait /home/${relativeFile}",
            "args": [],
            "presentation": {
                "reveal": "never",
                "panel": "shared"
            },
            "problemMatcher": [
                "$eslint-compact"
            ]
        }
    ]
}