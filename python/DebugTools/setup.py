from setuptools import setup, find_packages

setup(
    name='dtools',
    version='0.1.0',
    packages=find_packages(include=['dtools']),
    description='Simplifies development in Visual Studio Code Debug Console',
    author='Matthias Hermann',
)
